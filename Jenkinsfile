#!/usr/bin/env groovy
@Library('jenkins-shared')_


pipeline{
      agent any 

      def gv

      stage("init") {
            steps {
                script {
                    gv = load "script.groovy"
                }
            }
        }
      stage('Testing the Groovt')
      {
            
            printMsg()
      }
}